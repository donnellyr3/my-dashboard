services:
  - type: web
    name: my-dashboard
    env: python
    plan: free
    region: oregon
    buildCommand: pip install -r requirements.txt
    startCommand: python3 app.py
    envVars:
      - key: SCRAPEOPS_API_KEY
        value: demo-key
    autoDeploy: true

